local colors = require('bluloco.colors')

return {
    bg = tostring(colors.blue_grey),
    black = tostring(colors.black),
    skyblue = tostring(colors.light_blue),
    cyan = tostring(colors.braker_blue),
    fg = tostring(colors.light_grey),
    green = tostring(colors.dark_green),
    oceanblue = tostring(colors.off_blue),
    magenta = tostring(colors.magenta),
    orange = tostring(colors.dark_dandelion),
    red = tostring(colors.off_red),
    violet = tostring(colors.light_purple),
    white = tostring(colors.pure_white),
    yellow = tostring(colors.sun),
}
