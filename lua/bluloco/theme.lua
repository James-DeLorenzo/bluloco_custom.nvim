local colors = require('bluloco.colors')

return {
    bg = colors.blue_grey,
    black = colors.black,
    skyblue = colors.light_blue,
    cyan = colors.braker_blue,
    fg = colors.light_grey,
    green = colors.dark_green,
    oceanblue = colors.off_blue,
    magenta = colors.magenta,
    orange = colors.dark_dandelion,
    red = colors.off_red,
    violet = colors.light_purple,
    white = colors.pure_white,
    yellow = colors.sun,
}
